{
  "name": "automation-framework-root",
  "private": true,
  "scripts": {
    "postinstall": "npm run prepare-engine",
    "setup": "node setup-windows.js",
    "setup-windows": "node setup-windows.js",
    "prepare-bins": "node prepare-sidecar.js",
    "prepare-engine": "node prepare-engine.js",
    "verify": "node verify-setup.js",
    "diagnose": "node diagnose.js",
    "dev": "npm run prepare-bins && npm run prepare-engine && cd tauri-app && npm run tauri dev",
    "build": "npm run prepare-bins && npm run prepare-engine && cd tauri-app && npm run tauri build"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
